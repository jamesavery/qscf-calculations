# This file defines parameters for the Single Electron Transistor environment.

# SET-environment parameters
$dielectric_constant = 10.0;	# Dielectric constant of gate oxide.
$dist_x = 1;		# Distance in AA from either electrode to
			# nearest nucleus.
$dist_y = 1;		# Distance in AA from gate oxide surface to
			# nearest nucleus.

$oxide_height    = 50;	# Thickness of gate oxide in AA.

# Dimensions in AA for vacuum calculation
$vacuum_box_size = 2000;	
